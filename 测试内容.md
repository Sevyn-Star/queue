# 商场排队点餐 — 测试内容

后端地址：`http://127.0.0.1:3001`  
小程序目录：`源码/zhuye`

## 一、自动化接口测试（已执行）

启动后端后执行：

```bash
cd backend
npm start          # 另开终端
npm test           # 本目录跑 42 条接口用例
```

**最近一次结果：以 `npm test` 为准**

覆盖范围：

| 模块 | 用例 |
|------|------|
| 公开接口 | 健康检查、餐厅列表/详情/菜单、搜索、公告、静态图片 |
| 鉴权 | 未登录拒绝、口令错误、前端自称管理员无效、预设管理员登录、越权访问 |
| 用户 | 取号、预约、我的排队、提交订单、到店、取消 |
| 商家 | 店铺信息、排队、叫号、订单、菜品增删改、今日统计 |
| 管理员 | 看板、申请列表、驳回、公告增删改 |

## 二、测试账号（小程序）

开发者工具需勾选：**不校验合法域名、web-view、TLS**。

登录不再选择身份，角色由数据库决定。

| 账号 | 操作 | 预期 |
|------|------|------|
| 普通用户 | 头像 + 任意昵称，口令留空 | 身份为普通会员，可「商家入驻」 |
| 演示管理员/商家 | 昵称 **小柒**，管理员口令 **admin123** | 识别为管理员·商家，「我的」出现商家中心、管理后台 |

## 三、小程序手工用例

### A. 用户端

1. **首页**  
   打开小程序 → 看到轮播公告、热门餐厅、按楼层排队概览。  
   点「稻香寿司」进入详情，菜品图能显示。

2. **搜索**  
   搜「寿司」有结果；搜「xyz不存在」提示未搜到。

3. **取号**  
   排队 Tab → 选「云味小馆」→ 立即取号 → 选「直接取号」→ 「我的」里出现 waiting 号码。

4. **点餐取号**  
   选餐厅 → 点餐加菜 → 提交订单 → 同时生成排队号和订单。

5. **预约**  
   预约 Tab → 选店、选时间 → 提交 → 状态为已预约。

6. **取消 / 到店**  
   对预约点取消；对排队点确认到店。未登录时取号应提示先登录。

### B. 商家端（小柒）

7. 首页展示营业状态、今日客流/营收。  
8. 排队管理：叫下一号，队列减少。  
9. 订单：能看到关联订单；可标记完成/过期。  
10. 菜品：新增一道测试菜，改状态，再下架。  
11. 店铺信息：改公告后保存，首页详情能看到。

### C. 管理端（密码 admin123）

12. 看板：用户数、入驻商家数有数字。  
13. 商家申请：待审列表可驳回/通过。  
14. 公告：新建一条，编辑，删除。首页轮播应跟着变。

## 四、测试数据（种子库）

`npm run seed` 会重置数据库，导入 `数据库/` 与 `存储数据/`。

- 餐厅 4 家：稻香寿司、海底捞、麦门汉堡、云味小馆  
- 菜品 16 道  
- 公告 3 条  
- 商家账号：小柒（云味小馆）

注意：部分 JSON 里的云存储文件名，在 `存储数据/` 里没有对应文件（例如云味小馆店铺头像 `1764995880993_shop.png`）。稻香寿司/海底捞/麦门汉堡首页图是齐的。重新 seed 会清掉自动化测试新写入的排队和订单。

## 五、已知限制（手工时注意）

- 真机预览要把 `app.js` 的 `baseUrl` 和 `backend/.env` 的 `PUBLIC_URL` 改成电脑局域网 IP，再 `npm run seed`。  
- 模拟器才能直接访问 `127.0.0.1`。  
- 未做微信支付、短信验证码。
